test.todo('postgres migration')
/*import { associatedPartnerManifests } from '../../partnerManifests'
import getAssociatedPartnerOrgByKey from '../../utils/get-associated-partner-by-key'

describe('getAssociatedPartnerOrgByKey', () => {
  test('Should find associated partner org from a volunteer partner org', () => {
    const result = getAssociatedPartnerOrgByKey(
      'volunteerPartnerOrg',
      'example'
    )
    const expected = associatedPartnerManifests.example

    expect(result).toEqual(expected)
  })

  test('Should find associated partner org from a student partner org', () => {
    const result = getAssociatedPartnerOrgByKey('studentPartnerOrg', 'example')
    const expected = associatedPartnerManifests.example

    expect(result).toEqual(expected)
  })

  test('Should find associated partner org from a student sponsor org', () => {
    const result = getAssociatedPartnerOrgByKey('studentSponsorOrg', 'sponsor')
    const expected = associatedPartnerManifests.sponsor

    expect(result).toEqual(expected)
  })

  test('Should return undefined if cannot find an associated volunteer partner org', () => {
    const result = getAssociatedPartnerOrgByKey('volunteerPartnerOrg', 'bogus')
    expect(result).toBeUndefined()
  })
})
*/
